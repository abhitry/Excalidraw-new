apiVersion: v1
kind: ConfigMap
metadata:
  name: excelidraw-config
  namespace: excelidraw
data:
  # Frontend environment variables
  NEXT_PUBLIC_HTTP_BACKEND: "https://excelidraw.abhishek97.icu/api"
  NEXT_PUBLIC_WS_URL: "wss://excelidraw.abhishek97.icu/ws"
  
  # Backend ports
  HTTP_PORT: "3001"
  WS_PORT: "8080"
  
  # Database configuration
  POSTGRES_DB: "excelidraw_app"
  POSTGRES_HOST: "excelidraw-postgres"
  POSTGRES_PORT: "5432"